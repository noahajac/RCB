<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.noahjacobson.rcb.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:id="@+id/root_status_text"
        android:layout_centerHorizontal="true"
        android:layout_alignParentTop="true"
        android:textSize="20sp" />

    <Button
        android:text="@string/disable_root_button_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:id="@+id/disable_root_button"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/root_status_text"
        />

    <Button
        android:text="@string/enable_root_button_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:id="@+id/enable_root_button"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/disable_root_button"
        android:layout_alignStart="@+id/disable_root_button"
        android:layout_alignEnd="@id/disable_root_button"/>

    <Button
        android:text="@string/toggle_root_button_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:id="@+id/toggle_root_button"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/enable_root_button"
        android:layout_alignStart="@id/enable_root_button"
        android:layout_alignEnd="@+id/enable_root_button" />

</RelativeLayout>
